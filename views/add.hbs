<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thêm sản phẩm</title>
    <link rel="stylesheet" href="/path/to/your/css/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .form-container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        form {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            width: 100%;
            max-width: 800px;
            margin: 20px 0;
        }

        form > div {
            flex: 1;
            min-width: 150px;
            margin: 10px;
        }

        input, select {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            box-sizing: border-box;
        }

        input[type="submit"] {
            width: auto;
            background-color: blue;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            padding: 10px 20px;
        }

        input[type="submit"]:hover {
            background-color: darkblue;
        }

        h1 {
            text-align: center;
        }
    </style>
</head>

<body>
    <div class="form-container">
        <h1>Thêm sản phẩm</h1>

        <form action="/add-product" method="post">
            <div>
                <input placeholder="Nhập tên" name="name" required />
            </div>
            <div>
                <input placeholder="Nhập giá" name="price" required type="number" min="0" />
            </div>
            <div>
                <input placeholder="Nhập số lượng" name="quantity" required type="number" min="1" />
            </div>
            <div>
                <input placeholder="Nhập URL hình ảnh" name="image" required />
            </div>
            <div>
                <p>Loại sản phẩm</p>
                <select name="category" required>
                    {{#each listCate}}
                    <option value="{{_id}}">{{name}}</option>
                    {{/each}}
                </select>
            </div>
            <div>
                <input type="submit" title="Submit" value="Thêm sản phẩm" />
            </div>
        </form>
    </div>
</body>

</html>
